---
apiVersion: k8s.native-lb/v1
kind: Cluster
metadata:
  labels:
    controller-tools.k8s.io: "1.0"
  name: cluster-external
  namespace: nativelb
spec:
  # Add fields here
  default: false
  internal: false
  subnet: "11.0.0.0/24"
